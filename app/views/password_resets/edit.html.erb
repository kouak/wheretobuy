<h1>Change my password</h1>

<% semantic_form_for @user, :url => password_resets_path(:token => params[:token]), :method => :put do |f| %>
  <%= f.error_messages %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.submit "Change my password and log me in" %>
<% end %>