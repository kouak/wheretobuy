<% title "Brands" %>

<table>
  <tr>
    <th>Name</th>
    <th>Url</th>
  </tr>
  <% for brand in @brands %>
    <tr>
      <td><%=h brand.name %></td>
      <td><%=h brand.url %></td>
      <td><%= link_to "Show", brand %></td>
      <td><%= link_to "Edit", edit_brand_path(brand) %></td>
      <td><%= link_to "Destroy", brand, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Brand", new_brand_path %></p>
