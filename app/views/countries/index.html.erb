<% title "Countries" %>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% for country in @countries %>
    <tr>
      <td><%=h country.name %></td>
      <td><%= link_to "Show", country %></td>
    </tr>
  <% end %>
</table>